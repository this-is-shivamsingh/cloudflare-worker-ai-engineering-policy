services:
  app:
    build:
      context: .
    init: true
    ports:
      - "127.0.0.1:${HOST_PORT:-5173}:5173"
    volumes:
      - type: bind
        source: ./.dev.vars
        target: /run/secrets/cloudflare_dev_vars
        read_only: true
        bind:
          create_host_path: false
